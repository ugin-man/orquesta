{
  "$id": "event-batch", "type": "object", "required": ["sequence", "expected_revision", "batch_id", "actor", "correlation_id", "events"],
  "properties": {
    "sequence": { "type": "integer", "minimum": 0 }, "expected_revision": { "type": "integer", "minimum": 0 }, "batch_id": { "type": "string", "pattern": ".+" },
    "actor": { "type": "object", "required": ["type", "id"], "properties": { "type": { "enum": ["agent", "user", "system"] }, "id": { "type": "string", "pattern": ".+" } }, "additionalProperties": false }, "correlation_id": { "type": "string", "pattern": ".+" },
    "events": { "type": "array", "minItems": 1, "items": { "type": "object", "required": ["event_id", "schema_version", "type", "payload", "evidence_refs"], "properties": { "event_id": { "type": "string", "pattern": ".+" }, "schema_version": { "type": "integer", "minimum": 1 }, "type": { "type": "string", "pattern": ".+" }, "payload": { "type": "object", "additionalProperties": true }, "evidence_refs": { "type": "array", "items": { "type": "string", "pattern": ".+" } } }, "additionalProperties": false } }
  }, "additionalProperties": false
}
