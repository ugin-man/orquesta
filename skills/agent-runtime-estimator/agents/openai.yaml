interface:
  display_name: "Agent Runtime Estimator"
  short_description: "Estimate AI task runtime without human-time anchoring."
  default_prompt: "Use $agent-runtime-estimator to estimate this bounded task in AI execution time with P50/P80 uncertainty. Prefer one compact calibration profile if available; do not load raw runtime history."
