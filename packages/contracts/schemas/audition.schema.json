{
  "$id": "audition", "type": "object", "required": ["audition_id", "candidate_id", "hypothesis", "sandbox", "steps", "expected_evidence", "observed_evidence", "side_effects", "verdict", "cleanup_status"],
  "properties": {
    "audition_id": { "type": "string", "pattern": ".+" }, "candidate_id": { "type": "string", "pattern": ".+" }, "hypothesis": { "type": "string", "pattern": ".+" }, "sandbox": { "type": "string", "pattern": ".+" },
    "steps": { "type": "array", "minItems": 1, "items": { "type": "string", "pattern": ".+" } }, "expected_evidence": { "type": "array" }, "observed_evidence": { "type": "array" }, "side_effects": { "type": "array" },
    "verdict": { "enum": ["pass", "fail", "inconclusive", "blocked"] }, "cleanup_status": { "enum": ["clean", "pending", "blocked"] }
  }, "additionalProperties": false
}
