"use strict";

const { correlateEvidence, eventType } = require("./correlate");
const { normalizeEvidence } = require("./normalize");

module.exports = { correlateEvidence, eventType, normalizeEvidence };
